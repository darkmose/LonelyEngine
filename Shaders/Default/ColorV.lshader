#version 330 core
layout (location = 0) in vec3 pos; 

struct MatrixMVP
{
	mat4 model;
	mat4 view;
	mat4 projection;
};
uniform MatrixMVP Matrix;

void main()
{
	gl_Position = Matrix.projection * Matrix.view * Matrix.model * vec4(pos,1.f);
}
