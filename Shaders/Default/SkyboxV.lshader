#version 330 core
layout (location = 0) in vec3 pos; 

uniform mat4 projection;
uniform mat4 view;

out vec3 TexPos;

void main()
{
	TexPos = pos;
	vec4 poss = projection * view * vec4(pos,1.f);
	gl_Position = poss.xyww;
}
