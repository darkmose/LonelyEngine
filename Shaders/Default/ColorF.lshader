#version 330 core

out vec4 color;

struct props
{
	vec3 objectColor;
};

uniform props Props;
	
void main()
{
if(gl_FragCoord.x < 640)
    color = vec4(Props.objectColor,1.f);
else
	color = vec4((vec3(1)-Props.objectColor),1.f);
}

